# 3DMk pre-merge baseline

**Captured:** 2026-07-18 19:36:20 America/Cancun  
**State:** before application implementation changes; after approval of the VWM merge plan

## Recovery point

- Archive: `C:\Users\Scott Birse\Documents\3DMk 2.0 Archives\3DMk-2.0-pre-vwm-merge-20260718-193300.7z`
- SHA-256: `1089BB8B7D366939AB59A48B905D25728D4BBAF4FE65B64DA9D3A15BF131631B`
- Verification: 7-Zip reports `Everything is Ok` for 929 files and 204 folders.
- Regenerable `target`, nested `.git`, `node_modules`, and `__pycache__` directories were excluded.

## Toolchain

- Rust: `rustc 1.94.1 (e408947bf 2026-03-25)`
- Cargo: `cargo 1.94.1 (29ea6fb6a 2026-03-24)`
- Host: `x86_64-pc-windows-msvc`
- PDAL: not installed or on `PATH`.

## Dependency truth

Root `Cargo.toml` still resolves only three VWM crates and points them at the obsolete tree:

- `vwm-workspace/VWM-Repo/crates/vwm-core`
- `vwm-workspace/VWM-Repo/crates/vwm-io`
- `vwm-workspace/VWM-Repo/crates/vwm-geometry`

The corrected canonical workspace is `VWM-Repo-Implicit` and contains eight crates. The dependency-path switch is the first implementation change.

## Runtime route baseline

- `GET /api/health`
- `POST /api/pdf-to-3d`
- `POST /api/poisson-reconstruct`
- `POST /api/point-cloud-to-mesh`
- `POST /api/point-cloud-analyze`
- `POST /api/flat-surface-correct`
- `POST /api/mesh-smooth`
- `POST /api/image-assisted-refinement`
- `POST /api/model-floorplan-recognize`
- `GET /output/*`
- `GET /*` static frontend

There is no `/api/v1/capabilities` route, project/revision API, job API, package API, recognition API, or image-projection API.

## Capability truth before correction

| Capability | Legacy health claim | Actual maturity |
|---|---|---|
| Point cloud to mesh | Runtime PDAL Boolean | Unavailable on this machine; PDAL is absent |
| Point-cloud analysis | `true` | Experimental deterministic geometry analysis |
| Flat-surface correction | `true` | Experimental rigid transform through a lossy upload/output path |
| Mesh smoothing | `true` | Experimental Rust position smoothing through a legacy upload path |
| Image-assisted refinement | `true` | Unavailable as named; implementation scores images and changes texture contrast |
| Model floor-plan recognition | `true` | Unavailable as a room solver; current implementation is a rejected single slice |
| Scene analysis context | `true` | Experimental and not revision-backed |
| Non-destructive cleanup | `true` | Unavailable as a committed cleanup workflow |
| Object recognition | Not reported | Unavailable; no packaged ONNX model/workflow |
| Project revisions | Not reported | Unavailable |
| Rust ZIP package ingest | Not reported | Unavailable |

## Test baseline

### Canonical VWM workspace

Command: `cargo test --workspace` in `VWM-Repo-Implicit`.

- Result: pass.
- Tests: 40 passed, 0 failed.
- Screened Poisson sphere test time dominated the suite at about 45.65 seconds.

### Root application

Command: `cargo test` in the root.

- Result: pass.
- Tests: 33 passed, 0 failed.
- Warnings: two unused imports originate in the obsolete `vwm-workspace` geometry crate.
- Existing tests assert the current slice-floorplan and contrast-refinement behavior; those tests must be replaced when the misleading capabilities are retired.

## Standalone/offline baseline

`public/index.html` loads runtime resources from:

- cdnjs for jsPDF and JSZip;
- jsDelivr for Three.js, Three addons, Gaussian Splats, and three-mesh-bvh;
- Google gstatic for Draco.

`src-tauri/tauri.conf.json` has `csp: null` and `bundle.active: false`. Compilation alone does not establish a working offline standalone executable.

## Fixture identities

The versioned machine-readable catalog is `tests/fixtures/manifest.json`. It covers the real 255,330-point PLY, the 161-entry iPhone capture, a real GLB, a textured OBJ/MTL/five-image package, two known-bad reconstruction OBJs, and two malformed ZIP inputs.

## Known product failures retained as negative evidence

1. PDAL-backed reconstruction cannot run locally.
2. The existing implicit audit outputs are visually unacceptable and must fail future quality gates.
3. Level/smooth legacy output cannot be considered attribute-safe merely because the browser retains some in-memory material state.
4. The slice-based floor plan does not prove walls, corners, or rooms.
5. Structure heuristics are not object recognition.
6. The current image endpoint is not calibrated projection or geometry refinement.
7. Tauri depends on remote frontend assets and has no active release bundle.
