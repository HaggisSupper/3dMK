# Materialization requested

This temporary marker triggers the repository workflow that expands the previously merged transport archive into the real source tree. The workflow removes the bootstrap payload and this marker is overwritten or removed by the extracted platform tree.
