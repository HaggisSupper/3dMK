[package]
name = "spatial-engineering-desktop"
version = "0.1.0"
edition = "2021"
[build-dependencies]
tauri-build = { version = "2" }
[dependencies]
tauri = { version = "2" }
spatial-types = { path = "../../../crates/spatial-types" }
[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
